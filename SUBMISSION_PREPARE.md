# 零匹配论文提交准备

这条流程只处理 `list.xlsx` 中匹配数量 **明确等于 0** 的行。匹配数为 1 或多条的记录留给原来的核对、认领功能。空值、公式、不合法数量会报错，不会当成 0。

本次交付为代码和四份原始空模板，未针对真实论文运行分类、检索、下载或填表。程序准备的是待核验材料，不执行网页上传、推送、全库操作或完成标记回写。

## 统一工作台

双击 `启动助手.vbs`，打开 **零匹配提交准备**：

1. 默认提供四份模板；程序发现所选目录中的所有 `.xlsx` 空模板，以文件名作为类型，不限四类。新增模板同样使用第 1 行说明、第 2 行表头，并在配置的 `required` 中声明关键字段。
2. 点击“检查名单与模板”可检查零匹配任务数量及模板结构。这一步不联网、不使用 token。
3. 可选填补充来源配置 JSON，用于提供已有出版记录或真实数据库导出地址。
4. 把手动导出的 txt/csv/xlsx 放进 `runtime/submission/待收导出/`（程序自动创建）。点击“开始 / 继续准备”时，待收目录里能匹配到题名或 DOI 的文件**优先**被采纳，其次才尝试配置的数据库导出地址。下载或采纳成功即保留原文件，不请求公开元数据、不调用 AI、不要求模型密钥。
5. 两者都拿不到时才检索公开元数据、调用 API 填写模板；仍不完整的记录会得到一份**预填模板**——题名、DOI、WOS 记录号等已确定字段先填好，其余列留空，放在「待补草稿」里由你手动补齐。
6. 从“打开材料目录”查看来源总表、分类型齐备材料、待补草稿，以及没能匹配上的待收文件清单。

相同题名及 DOI 只请求一次，但总表保留全部零匹配原行号；同题记录中匹配数非零的行不会混进提交队列。已有完成备注不自动排除：本流程严格按匹配数量筛选，提交前仍需查重。

## 来源、AI 与完整性

- 默认按 DOI，或无 DOI 时按题名检索 [Crossref 元数据 API](https://www.crossref.org/documentation/retrieve-metadata/rest-api/)。只接收唯一、题名规范化后一致且 DOI 不冲突的记录；近似匹配、多个同题结果交给待补处理。
- Crossref 是元数据来源，不等于 WOS、CNKI、EI 或其他数据库的收录证明。当前未提供这些数据库的检索凭据，不自动登录机构数据库。
- API 模型根据来源选择目录中已有的模板；默认提供期刊论文、会议论文、科技论文、著作章节。没有对应模板或无法确认类型的记录留在来源总表，不能强行套模板。新增类型需要来源明确支持该类型，不能仅按题名猜测。
- 每个字段必须引用来源编号，填写值必须与该来源同名字段一致。AI 不能凭记忆补作者、单位、DOI、日期、摘要或数据库记录号。来源中存在且无歧义的字段，即使 AI 漏填，代码也会补回并记录来源。
- 题名、作者、作者单位、发表日期、出处等为默认关键字段；原名单有 DOI 时不可漏填。检查作者与单位编号、真实日期，以及关键来源冲突。缺少信息时只导出草稿，不以“未知”“待补”填充正式字段冒充完整。
- 这些关键字段规则是**程序的完整性门槛**，不是声称平台已公布相同必填规则。可通过配置调整。四份模板本身未显示必填标记或验证规则。
- 期刊增加“发表期刊”；会议增加“会议名称、会议录名称”；科技论文增加“出处”；著作章节增加“发表期刊、出版者、页码”。提供的章节模板用“发表期刊”作为容器列，代码保留此原始字段名，实际值需来自对应书籍/集刊来源。
- 模板第 1 行说明、第 2 行表头及列顺序保留。重复的“会议日期”“WOS研究方向”等列仍保留，并写入同名字段值。多值字段使用英文分号，作者/单位按模板的编号方式关联。

字段齐备仍不证明本校成果归属、无重复、来源真实性或网站可导入。总表明确标为 **字段齐备待核验**。本流程也不自动填写工号、产权排序、内部平台编号、第一单位或通讯作者等没有依据的信息。

## 与 AI 分类结果衔接

本流程会自动读取 `runtime/classification/` 中**与当前名单指纹一致、已分类条目最多的一份**`分类结果.json`（只看 sha256 匹配的任务，不会混用其他名单的结果；条目数相同时取最新的一份，避免被中途放弃的少量结果覆盖），并只用它做两件事：

1. **预选模板**：分类给出的成果类型若正好是模板目录中的类型（期刊论文、会议论文、科技论文、著作章节等），该记录即使还没有出版来源，也会获得对应类型的预填模板。
2. **排导出渠道优先级**：配置了多个导出地址时，先试与推荐渠道一致的那个；同组内仍按配置顺序。

AI 分类是建议而不是证据：以它预选模板的记录会在总表「类型依据」列标明“AI 分类建议”，并在问题列写明“未经出版来源核实；预填模板需人工确认”，状态保持“待补资料”，不会因此变成“字段齐备待核验”。一旦有出版来源确认了类型，就以来源为准并标记“出版来源”。两者都无法确定类型的记录不会生成预填模板，只在总表中列出并注明原因。

## 待收导出目录与预填模板

程序不能代替你登录 CNKI、EI、万方、VIP 等数据库——它们没有可用的免登录导出接口，也不会绕过登录页。因此“自动导出”的边界是：**凡是能拿到的导出文件，程序都会自动识别、校验并采纳**。

把从任意渠道导出的 `.txt`、`.csv`、`.xlsx` 放进 `runtime/submission/待收导出/`，程序会：

- 按内容匹配题名或 DOI，把命中的文件复制进该任务的「原始导出」目录并记录校验值；
- 一个文件可以同时命中多篇论文（批量导出），不会重复下载；
- 内容是网页/登录页、判断题名不符、超过 20MB 或无法读取的文件一律不采纳，原因写入「待收未匹配.json」；
- 采纳成功即跳过公开元数据检索和 AI 调用，不消耗模型额度。

待收文件没有经过渠道校验，「导入渠道」列会留空，原始文件仍标记为“原始导出待核验”。

**下载不到也不会卡住**：只要成果类型可以确定，记录就会得到一份预填模板。已知字段（题名、DOI、WOS 记录号，以及 AI 或补充来源给出的值）先写进对应列，其余列留空——在「待补草稿」中直接补齐关键字段即可提交，不需要自己从头建表。

## 输出

每个输入、模板、模型和配置组合保存到独立的 `runtime/submission/<任务指纹>/`：

| 文件 | 内容 |
|---|---|
| 零匹配队列.json | 筛选后的任务编号、题名、DOI、零匹配原行号 |
| 提交准备与来源.xlsx | 提交准备总表（含「预填模板」「类型依据」列），以及每个填写字段对应的值、来源编号和出处 |
| 字段齐备/*.xlsx | 目录中各类原模板，只写入满足完整性检查的记录；暂无记录的文件仅保留模板头 |
| 待补草稿/*.xlsx | **预填待填模板**：已明确类型但字段不完整或冲突的记录，已知字段已先填好，其余留空 |
| 原始导出/* | 从配置链接或待收目录取得的真实 Excel/Txt/Csv 原始字节，不转换为伪数据库导出 |
| 待收未匹配.json | 待收目录中未能采纳的文件及原因（未匹配、是网页、过大或无法读取） |
| 提交准备.json | 完整结果、来源证据、缺失项及检索过程问题 |
| progress.json / status.json | 断点与运行状态（含 `prefilled` 预填模板数与 `inbox_unmatched` 未匹配数） |

每条处理后立即保存。重复启动默认跳过已处理项；勾选“重新处理待补资料的条目”会重新检索这些条目并调用 AI，消耗额度。换模板、修改配置或名单会建立新任务。原始输入与模板不覆盖。

## 命令行

```powershell
python submission_prepare.py --plan-only
python submission_prepare.py --model deepseek-chat
python submission_prepare.py --manifest submission_sources.json --retry-incomplete
python submission_prepare.py --classification runtime\classification --inbox runtime\submission\待收导出
```

`--plan-only` 生成准备计划供检查（含每条的分类提示），不联网或填表。还支持 `--input`、`--templates`、`--output`。`--classification` 默认指向 `runtime/classification`，`--inbox` 默认指向 `runtime/submission/待收导出`。

## 补充来源和原始下载配置

先从准备计划中的 `roster.papers` 获取真实任务 `id`。以下是配置格式示例，不是实际论文数据；替换示例 ID 后使用：

```json
{
  "sources": {
    "真实任务id": [
      {
        "id": "S1",
        "provider": "出版方记录（人工提供）",
        "url": "https://publisher.example/article",
        "kind": "期刊论文",
        "fields": {
          "题名": "与名单对应的题名",
          "DOI": "10.xxxx/example",
          "作者": "Author One(1);Author Two(1,2)",
          "作者单位": "(1)University A;(2)Institute B",
          "发表日期": "2025-03-01",
          "发表期刊": "Journal Name",
          "摘要": "来源原文摘要"
        }
      }
    ]
  },
  "download_hosts": ["exports.example.org"],
  "exports": {
    "真实任务id": {
      "channel": "WOS",
      "format": "txt",
      "url": "https://exports.example.org/real-export.txt"
    }
  }
}
```

补充资料须与论文题名/DOI一致，并提供出处。补充链接用于追溯，程序不自动访问它；这里的字段由提供者负责核验，AI只整理这些已给出的值。需要下载的真实导出地址放在 `exports` 中，与 `sources` 区分。

原始下载只接受配置主机的 HTTPS 直链，拒绝自动重定向、登录页、过大文件、错误格式，以及找不到对应题名/DOI的文件。不发送交大模型 token、Cookie 或浏览器会话到下载站点。需要登录、链接失效或无下载权限时，会记录为待核查，不绕过登录。原文件可能包含多条记录，下载后的内容条数与实际导入范围仍须核验。

`exports` 的每个任务可填写单个地址对象，也可填写按尝试优先级排列的对象数组，例如先 WOS、再 EI。第一个下载成功的文件将被采用；失败原因保留在总表。原始文件标记为“原始导出待核验”，并记录处理途径、导入渠道、数据库导出来源；不会再为同一任务生成填写记录。继续运行会核对已下载文件的存在性及校验值，丢失或改变则重新处理。

这里的“下载优先”是优先使用已经拿到的真实导出文件，顺序为：**待收目录中匹配到的文件 → 配置的导出链接（按分类渠道优先）→ 公开元数据与 AI 填表 → 预填模板待人工补齐**。尚未实现自动登录数据库、搜索论文或生成导出链接；未配置链接且待收目录为空时会明确记录原因，再进入模板流程，不声称论文未被数据库收录。

可选 `required` 按模板名称覆盖关键字段列表；内置四类使用默认值，新增类型必须声明非空列表，例如 `"required": {"专利": ["题名", "申请号"]}`，字段必须存在于该模板。不同表头布局的模板需先增加适配支持，不能直接按当前两行表头结构使用。不改变 `DOI或来源网址` 和已有 DOI 不可遗漏的检查。

## 验证

```powershell
python -m unittest tests.test_submission tests.test_unified
```

测试只用合成 Excel、模拟元数据和模拟模型，不使用真实论文、不联网，不消耗 API 额度。
