# 人工处理版：实施边界

2026-09-25 保留人工审批；开放单条作者认领与 WOS 元数据流程，PDF 和批量自动化暂停。本文件取代旧版半自动界面约定。

## WOS 和自动分流补充（0.3.0）

`automation.py` 负责确定性分类、WOS TXT 校验和写前 SQLite 日志；`automation_panel.py` 接入现有 Tk 工作线程，按规则选择 WOS、人员查找、元数据人工编辑或暂停。问题分流不调用模型，不能把模型输出当作执行命令。

扩展使用三个明确绑定的标签页：SA 页维持本地桥接心跳；WOS/导入页仅执行对应白名单命令，重新配对会清空工作页绑定。新增权限只有 WOS 主机和 downloads；绑定/断开仅允许由扩展弹窗发起。后台 SHA-256 校验原始 TXT，再由现有 WOS TXT 上传控件处理，不读取后台认证头。

WOS 导入需要当前 SA 与名单题名/编号一致、零匹配、单篇完整记录、明确交大署名。缩略题名或缺少精确编号匹配时需人确认；编号冲突不可确认绕过。每次上传、导入、推送前先落盘意图，已提交意图只能回读，不自动重发。按完整说明扫描唯一批次，回读单篇题名、DOI/WOS，并从实时选项标签解析 PPT 指定组合查重规则，永不选整体覆盖。

记录推送已核验不等于任务已完成：平台号核实、其他差异、网页备注与 Excel 批准保持独立。旧版无导入能力/权限不变的描述仅适用于下面的历史阶段。0.3.0 已经过合成回归，生产登录会话验收仍未完成。

## 人工工作台与既有保护

1. 只读取 `code/list.xlsx`。完成标记定位：一个「备注」列，或当前模板的首列「备注」紧邻「负责人」；其他歧义停止。
2. 数字 1 表示完成；文本 1、布尔值等不等同完成。「未完成／已完成」切换当前负责人的列表，数据行完整留在 Excel。已完成项能查看和定位，不能重复批准。
3. 人工页可显式启动本机 Bridge 并配对扩展，仅响应用户点击的 search/open_metadata/open_claim。编辑／认领需要当前任务的查询快照，核对网页 ID，不自动保存。未连接仍可管理本地名单。自动化页另外提供 prepare_claim（只读查找）及 submit_claim（经人工确认的单条作者认领），不暴露历史平台号/状态自动写入。
4. 人工勾选、确认目标 ID 后才回写 1。文字备注进入本地日志；另一列网页备注不改动。未勾选、取消、未知异常不写入。
5. 回写由后台线程执行，期间禁用切换任务和提交。核对源文件指纹，应用锁避免两个助手同时写，Excel 占用标记存在时停止；临时文件回读、备份校验后替换，并更新文件指纹以支持下一次处理。
6. 单元格级 OOXML 更新保留其他 ZIP 成员原始内容。正确区分自闭合空单元格，复制 ZIP 元信息，且校验所有任务记录只有目标完成标记变化。未知 XML 结构、签名、保护、合并单元格或元数据暂停，不绕过限制。无需重新生成整表。
7. Excel 是进度的唯一判定来源；旧 SQLite 的已完成记录不决定是否隐藏。若保存后日志失败，显示完成并报告日志失败；若结果未知，必须重读，不重试网页操作。
8. 窗口默认置顶，560×700，可缩到 520×600。黄色待办、绿色完成，且同时有文字标签。任务统计作为切换标签；批准按钮使用绿色主按钮风格，未核验／忙碌／已完成时禁用。操作按钮按文字宽度排列，长说明按需展开；完成后不自动转到下一条。
9. 名单、备份、运行状态不上传。Git 关键更新前先上传前一阶段，凭据仅在内存中用于指定 GitHub 仓库。

## 单条认领的两阶段边界

1. 从「认领状态」SA 列提取唯一括号中的数字文本，并与已有名单/列表工号交叉检查；不对工号做数值转换。
2. prepare_claim 重查唯一条目，调用 CompareDetailDrawer 的 claimDetail 打开作者页；使用其 authorModal 按工号查找。初始姓名查询结束后才发编号查询，避免旧异步结果混入；要求新结果数组、总数 1、返回工号逐字相同。
3. 返回学者快照和论文作者列表。精确姓名/别名仅用于建议，不能唯一对应时由人选择；确认对话框展示人员、编号、题名、作者序号与署名。
4. submit_claim 重新检查名单、列表、SA 来源、作者和人员快照。对象键顺序不作为数据变化；数组顺序、字段类型及内容变化均停止。人工作品否认、已有有效认领或非本机构行不自动覆盖。
5. 选人后校验绑定的学者 ID、工号与署名；调用既有单行 handleClaim 一次，绝不调用批量认领。只有 getItemDetail 替换新结果后，精确回读目标作者与学者关系才报告成功。提交后未知结果禁止自动重试，窗口留给人工核验。
6. 成功仅追加本地「已认领」备注；网页完成状态、网页备注、Excel 数字 1 仍是独立人工步骤。切换任务及错误会清除预览，绝不沿用旧任务人员。
7. Windows 本机连接服务使用独占监听，禁止两个实例静默共享端口。扩展权限保持不变；测试副本使用独立随机端口，不影响用户助手。

实现依据：2026-09-23 只读核查网站公开前端模块 CompareDetailDrawer、claimDetail（组件名 itemDetail）及 AuthorModal，未调用生产写接口。真实登录页面工具连接不可用，现场验收尚待用户执行；离线测试不替代现场兼容性验证。

并发边界：这不是多用户共享文件数据库；不要用其他应用在回写时替换名单，也不要在认领命令执行中同时操作配对网页。页面快照冲突检查不是服务器端跨用户事务锁。
# 模型辅助核对补充（2026-09-23）

新增 `model_review.py` 和 `model_panel.py`，使用标准库 HTTPS 访问用户授权的交大模型端点。
模型层不依赖 Bridge、网页方法或名单写入模块，不能执行模型返回的工具调用。
模型输出严格验证 JSON、结论枚举、字段长度与来源编号；没有证据或仅有名单时降为证据不足。
这些是结构保护，不证明模型陈述真实。最终批准始终走原人工复核、确认与单元格回写流程。

发送范围固定为当前单条的允许字段与用户补充资料。原始网页内容作为不可信资料，不能覆盖系统核对规则。
密钥通过 Windows DPAPI 保存到忽略的 runtime 中，只作为固定端点的 Authorization 请求头使用。
不跟随重定向、不记录密钥/响应正文/思考过程，不将凭据传给扩展。

异步结果绑定当前记录 key、完成状态、网页快照、比对字段、补充证据和所选模型；回调再次检查名单未变。
生成请求不写 Journal 的任务状态，不覆盖人工备注，不改变完成按钮的审批条件。
不自动检索、下载、认领或上传；这些后续能力仍需独立接入和验收。
