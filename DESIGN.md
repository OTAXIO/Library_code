# 半自动 SA 比对工具：实施边界

先手动打开机构知识库后台并登录，桌面弹窗通过用户配对的浏览器扩展操作当前标签页。只在 SA 比对结果页面工作，不读取登录令牌，不实现或绕过系统加密。

1. 读取父目录最新修改的「数据比对结果*.xlsx」，忽略 Office 临时文件；显示选中的文件与时间，用户选择负责人后建立队列。
2. 精确搜索 sa_lzk表ID，核对唯一结果、题名、匹配数和状态。Excel 是待办来源，不把它当作事实判断依据。
3. 每条展示 PPT 工作规则。支持人工核验后自动填写平台唯一号、备注并设为已处理；元数据修改、认领、重复合并、导入等在原网页人工执行后由工具重新读取。
4. 未知查询方式、多个结果、字段变化、超时、登录过期、页面变化立即暂停。写入结果不明时禁止自动重试。
5. 使用本地 SQLite 保留检查点和操作结果，不改源 Excel。重启后先重新核验，不自动续写。
6. Git 仅跟踪 code 内代码、文档、合成测试；原始名单和运行记录不能上传。

当前后台结构来自 2026-09-22 对公开前端资源的只读检查，尚未在已登录生产页面验证。上线前须用一条已核验记录人工验收。

开发分段：初始化 → 数据与检查点 → 弹窗及浏览器适配器 → 测试与加固。各关键更新前先提交上一阶段。远程仓库由用户提供，未提供时只能本地留底，不声称已上传。
