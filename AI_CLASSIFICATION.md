# AI 批量分类

这个功能读取本目录 `list.xlsx`，调用交大模型服务完成论文分类，输出 Markdown 和结构化 JSON。分类由所选外部模型完成，不使用此前人工整理的分类结果。

## 打开使用

界面采用工作台布局：顶部显示名单记录、分类任务、已处理和渠道待判定数量；进度条按实际完成比例更新。启动时自动读取当前名单与所选模型的最近结果，不发起模型请求。可按题名/DOI 搜索、按推荐渠道筛选，点击论文查看完整理由和待补材料；底部可直接打开分类报告或渠道分组清单。切换模型时不会混用其他模型的结果。

1. 双击本目录的 **启动助手.vbs**，进入“批量分类 / 导入渠道”页；**启动AI分类.vbs** 也会进入同一工作台并直接选中此页。
2. 选择模型：`deepseek-reasoner`、`deepseek-chat`、`qwen` 或 `minimax`。
3. 密钥复用原助手“模型辅助”中已经保存的交大密钥。尚未配置时，点击“密钥设置”；用 Windows DPAPI 加密保存，不写进代码。
4. 连接校园网或 VPN，点击 **开始 / 继续分类**。此操作会发送名单中的题名、DOI 和生成的论文编号，产生模型服务用量；不发送负责人、工号、平台编号和工作备注。
5. 点击 **打开结果文件夹** 查看 `分类建议.md`、`导入渠道分类.md` 和 `分类结果.json`。

## 导入渠道

程序同时输出成果类型、来源库建议和导入渠道建议，三者分别存储。渠道按截图限定如下：

| 渠道 | 页面按钮及格式 |
|---|---|
| 数据导入 | 数据导入（截图未注明格式，需确认模板） |
| WOS | WOS数据导入(Excel)、WOS数据导入(Txt) |
| CSCD | CSCD数据导入(Txt) |
| CSSCI | CSSCI数据导入(Txt) |
| CNKI | CNKI数据导入(Excel/Txt) |
| 万方 | 万方数据导入（截图未注明格式） |
| EI | EI数据导入(Csv/Excel) |
| VIP | VIP数据导入(Excel) |
| SPOP | SPOP数据导入(Txt/Excel)，含义待确认，不推定为 SCOPUS |

模型对每篇给出一个候选渠道、理由和核实条件；不足以建议时保留“待判定”。推荐渠道是优先检索并准备导出文件的方向，不证明实际收录，也不会据此填入来源库。PUBMED、DBLP、SCOPUS 不在截图专用入口中，不能直接推荐为页面渠道。

WOS 两个按钮对应同一渠道的两种格式，没有导出文件时同时列出候选入口，不强行选 Excel 或 Txt。JSON 的 `import_route.selected_button` 保留为空，待取得符合要求的实际导出文件后再决定；程序不生成伪造的数据库导出文件，也不执行网站导入。

新增渠道字段会产生新的任务指纹，旧成果类型分类文件仍保留。`导入渠道分类.md` 按渠道分组，同时统计任务数与原 Excel 行数。

默认选择 DeepSeek Chat，每批 5 个题名/DOI 组合，每批成功立即保存。批量大小可在界面「每批」中选择（1/2/3/5/8/10），切换模型会恢复该模型的默认批量。模型预算按模型分档：`deepseek-reasoner` 的隐藏思考会占用输出预算且响应很慢，因此为每批 2 篇、单次请求最长 300 秒、输出上限 16384；`deepseek-chat` 为每批 5 篇、150 秒、8192；`qwen` 与 `minimax` 为每批 5 篇、180 秒、8192。点击“本批完成后停止”，会保存当前批次后停止。任务运行时关闭窗口，也会等待当前批次返回并保存。异常强制退出可能丢失当前尚未保存的批次，已经保存的批次不受影响。

再次点击开始会跳过已成功条目；输入文件、模型、提示词或补充材料发生变化，会自动建立新任务目录。原始名单不会修改，也不会更改原助手的完成状态。

窗口默认启用自动恢复。请求超时或失败时，**多篇批次会先拆小再试**，而不是原样重发同一个大批次——大批次往往正是超时的原因，重发只会再次超时并重复计费；拆到单篇后最多重试两次，仍失败则记录该篇并继续其他论文。返回不合格的批次同样自动拆小，单篇仍不合格时记录失败并继续。连续 6 次请求失败或不合格会停止整轮，避免模型不可用或批量过大时持续消耗额度；已保存的批次不受影响，可从断点继续。429 按服务要求等待，要求等待超过 120 秒则停止；密钥无效或权限不足时立即停止。超时后的重试可能额外消耗额度。下一次运行只处理未成功的条目。出现失败时报告“部分完成”，不会冒充全部成功。界面顶部「分类失败」计数和筛选可查看失败条目，选中后显示失败原因。命令行须加 `--auto` 才启用这套恢复策略；原助手的单条“模型辅助”仍不自动重试。

## 分类规则

- 使用需求中列出的 38 个成果类型及 17 个来源库值。
- 只有题名与 DOI 均相同才合并请求。题名相同但 DOI 不同，或一个有 DOI、另一个没有 DOI，会单独处理，保留全部原 Excel 行号。
- 每次读取一个含“题名”列的工作表；存在多个这样的工作表、题名缺失或题名/DOI 是公式时明确报错，避免静默漏行。
- 不修改原题名内容，仅去掉首尾空白；JSON 保存映射，后续可以展开回原始行并接入用户提供的模板。
- 模型必须为每个论文编号返回一次。格式错误、漏项、重复编号、未知类别、输出被截断或凭空引用证据时，该批不保存为成功。
- 推理模型（`deepseek-reasoner`）的思考 token 与正文共用同一个 `max_tokens`；批量过大时容易在预算内被截断，表现为 `finish_reason` 不是 `stop`，该批不采纳并自动拆小重试。实测每批 3–5 篇会间歇性超时或截断，1–2 篇稳定通过，因此该模型默认每批 2 篇。
- **此 API 没有联网检索能力，输出始终是“AI 建议待复核”。** 模型无法判断时类型为空，报表显示“待判定”。未提供明确数据库材料时，来源库留空；不能凭语言或 DOI 推断收录。
- JSON 中的 `confidence` 是模型自己的判断，不能当成已核实概率。提供材料得到的数据库值也仍需核验。

## 输出及续做

结果位于 `runtime/classification/<任务指纹>/`：

| 文件 | 用途 |
|---|---|
| 分类建议.md | 可阅读的分类表，包含未处理行、理由及缺失证据 |
| 导入渠道分类.md | 按截图渠道分组的候选清单、对应入口和需要核实的条件 |
| 分类结果.json | 论文编号、题名、DOI、原表行号和分类建议，供后续填模板程序读取；失败条目另带 `failure` 字段说明原因 |
| progress.json | 已完成批次，供中断后继续 |
| status.json | 当前状态、已完成/失败/待处理数量、本轮请求次数 |
| events.jsonl | 启动、请求、保存、等待重试和结束的事件记录，不含密钥或思考过程 |
| failures.json | 未获得合格结果的单篇编号、原行号及失败原因，超时与传输失败也会记录在此 |
| run.lock | 防止同一任务被两个进程同时执行，正常结束会删除 |

如果强制结束进程后提示任务仍在运行，先确认分类窗口和进程都已关闭，再移除对应任务目录里的 `run.lock`。不要在任务运行期间删除锁或修改进度文件。

## 命令行及补充材料

安装项目原有依赖 `requirements.txt` 后，在本目录运行：

```powershell
python paper_classify.py --prepare
python paper_classify.py --model deepseek-chat --batch-size 5 --auto
```

`--prepare` 只在输出根目录生成 `待分类名单.json`，不发起模型请求。可通过 `--input` 指定其他 Excel，通过 `--output` 指定结果根目录。没有独立环境时可使用 `启动AI分类.vbs` 自动找到本机已有的 Python。

需要补充出版记录或数据库依据时，先从 `待分类名单.json` 取得论文 `id`，编写如下材料文件（替换示例编号）：

```json
{
  "名单中的完整论文id": [
    {"id": "E1", "text": "这篇论文的出版或数据库记录摘录，以及出处 URL。"}
  ]
}
```

```powershell
python paper_classify.py --evidence evidence.json
```

材料只按论文编号匹配；程序不会自动打开材料中的 URL。未知编号会报错，不会把其他论文材料混入。

## 按分类导出 WOS 元数据

分类结果里推荐渠道为 WOS 的记录，可以在本页点 **「按分类导出 WOS 元数据」** 批量导出：

1. 需要先在人工处理页“连接浏览器”，并在扩展里单独绑定一个 WOS 工作页（与“自动化 / 认领”页共用同一套绑定）。
2. 程序只处理**匹配数为 0、尚未完成**的记录，逐条复用扩展的检索 → 导出「制表符 / 完整记录」流程；只选定唯一结果，身份不明确时暂停该条并继续下一条。
3. 捕获到的 TXT 会复制到 `runtime/submission/待收导出/`。已导出过的条目直接复用本地存档，不重复检索、不重复下载。
4. 连续 3 条导出失败即停止，避免标签页被切换、登录失效或未绑定时把整份名单走完（已成功的条目不受影响）。
5. 导出后还会再做一次**强身份校验**：名单里有 DOI 或 WOS 入藏号时必须与导出记录完全一致，且完整记录里必须有明确的上海交通大学署名。冲突或缺署名记为该条失败；只是**无法强确认**（例如名单只有题名、没有编号）的条目**不会放进待收目录**，只单独列出并保留在归档里，等你在“自动化 / 认领”页逐条核验——待收目录是自动采纳的，未经核实的结果不能进去。
6. 之后到“零匹配提交准备”页点“开始 / 继续准备”，这些 TXT 会被自动匹配并采纳，跳过公开元数据检索和 AI 调用。

**这一步只做下载。** 上传、导入和推送会写入生产库，仍需在“自动化 / 认领”页逐条人工确认执行，程序不提供批量导入或批量推送——批量导出中途出错也不会对库里产生任何写入。

## 验证

```powershell
python -m unittest tests.test_paper_classify tests.test_model_review
```

测试使用合成 Excel 和模拟模型响应，不发送真实论文，不消耗模型额度。另可用 `--prepare` 检查实际名单读取情况。
