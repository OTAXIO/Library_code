# 验证报告

日期：2026-09-22。备注规则更新版 0.1.1。结论：**61 项检查通过；真实已登录后台的首条写入验收仍待用户完成。**

## 验证范围

| 层级 | 数量 | 结果 |
|---|---:|---|
| Python：名单、连接、断点、备注规则和原生弹窗 | 28 | 全通过 |
| 浏览器适配器：合成页面分支 | 28 | 全通过 |
| 真实浏览器扩展 + Python 本地服务全链路 | 5 | 全通过 |

环境：Windows；Python 3.12.14；openpyxl 3.1.5；tkinter 8.6；Playwright 1.62.1；本机 Edge 的隔离无界面测试会话。

## 已验证

- 19 位平台号及工号前导零保留为文本；非文本编号、公式、重复名单 ID、匹配数不一致停止。
- 当前名单文件改变后不继续旧队列写入；读取失败/文件被占用交给用户处理。
- 负责人筛选隔离任务，默认未勾选核验；源 Excel 只读。
- 本地配对认证、第二标签页拒绝、离线拒绝；命令只投递一次。
- 查无结果、结果不唯一、ID 不相等、登录路由、字段变化、旧缓存、未知命令停止。
- 已打开的用户编辑弹窗不自动关闭。
- 修改前检查快照；跨浏览器消息的属性顺序差异不会造成误判。
- 只有人工核验与备注齐全才允许写入；已处理记录不会被切换回待处理。
- 平台号和状态/备注均通过回读验证；回读不一致进入未知状态而不是成功。
- 编辑/认领交给原网页；多条匹配不直接打开未指定条目的编辑器。
- 过期命令不写入；本地断点可在重启后读取。
- 真实扩展能加载、配对，并往返完成搜索、平台号写入、完成状态写入及重复完成拒绝。
- 用户指定的“已认领”“DOI和WOSID SA未提交”“通讯作者修正”按原文保留，可合并且不重复添加。
- “已认领”可作为三字标准备注提交；尚未认领时拒绝，不因缩短备注而放松完成条件。
- SA 双字段都为空才允许双缺失备注；只缺一个、字段缺失或重复时拒绝。
- 通讯作者标记未知时拒绝，人工核对后允许本库正确值与 SA 不同。
- 标准备注相关详情在用户确认后变化，即使列表摘要没有变化也停止提交。
- 原生弹窗测试覆盖备注按钮填入、去重、清除核验勾选及短备注通过；真实扩展测试覆盖三字“已认领”的端到端提交与回读。

## 本次迭代修复

1. 调整弹窗布局，保证主要按钮不被默认内容高度挤出窗口。
2. 执行命令时禁止切换左侧当前任务，避免显示目标与操作目标混淆。
3. 配对只握手，不消耗可能排队的操作命令。
4. 用浏览器提供的扩展来源 URL 验证配对弹窗身份。
5. 以字段集合及字段值核对快照，不依赖 JSON 属性顺序。
6. 暂停后不再触发已排队的“自动下一条”；清除修改前的陈旧详情展示。

## 证据边界

- 所有自动写入测试均使用**合成网页和合成记录**。没有执行真实后台写入，也没有自动登录。
- 公开前端组件结构已只读核查；这不是已登录接口权限和生产数据保存行为的证明。
- 原名单曾只读读取到 3,472 条、11 位负责人；用户工作目录中的文件可能在工作期间被其他进程更新，程序每次重新加载时以所选文件的当前内容为准。
- 本次代码没有写入原 PPT/Excel；不承诺其他应用没有修改这些文件。
- 测试不证明原文作者身份、第一单位、重复关系或论文归属；这些仍需人工证据。
- 首次上线请按 README 第 7 节只用一条已核验记录验收。权限、界面、备注字段回读若不兼容，工具应暂停，不能以离线通过为由强行继续。
