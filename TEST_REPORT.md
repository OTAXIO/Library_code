# 验证报告

日期：2026-09-23。当前版本为人工审批工作台，自动化停用。

## 本次验证

- Python 核心、保留的规则、名单回写和新界面共 **58 项检查，全部通过**。
- 其中 15 项针对单元格回写，12 项针对人工界面；其余为已有核心和规则回归。
- 原生 Tk 窗口在 560×700 和 520×600 两种尺寸核查主要控件边界；置顶属性开启，两页可切换，人工页无浏览器连接对象。
- 真实 `list.xlsx` 只读：3,472 条，26 条数字 1，完成标记为 A 列；其余 3,446 条按负责人进入待办。真实文件的单元格更新格式在内存中验证，未保存到原文件。

## 覆盖的关键分支

- 数字 1 隐藏；文本 1、0、布尔值保持待办；全部完成时是有效空队列。
- 两个备注列正确区分；缺失、重复歧义、公式等异常停止。
- 仅负责人自己的未完成任务出现，旧日志已完成不覆盖 Excel 状态。
- 勾选和确认均必需；取消不写入；忙碌时不能切换目标或重复提交。
- 完成后数字回读、绿色状态、移除待办，重启／重读仍隐藏；不会自动搜索或推进下一条。
- 备注模板去重，修改备注或切换记录后清除核验。
- 文件发生变化、Excel 占用、另一助手持锁、保护状态、备份失败和替换失败均停止。
- 替换失败保留原文件，清理本次临时文件；已有备份不删除。
- 连续两次完成刷新文件指纹；原备注可在备份恢复。
- 原始备份逐字节一致；XLSX 内除目标工作表外的所有成员内容不变。样式、长编号、另一备注列、其他工作表与公式保持。
- 保存失败保持待办；保存成功但日志失败显示真实完成结果并提示，不要求重复写入。
- 重读失败清除旧队列，防止继续使用失效名单。

## 验证边界

- 写入测试只使用临时目录中的合成记录，没有修改用户真实名单，没有操作真实网站。
- 窗口检查是原生控件布局与状态验证；尚未由用户在日常浏览器操作中验收各屏幕缩放比例下的体验。
- 历史浏览器扩展未在此次更改中启用或重测，其旧版离线通过结果不代表新版自动化已可用。
- 当前人工审批由用户判定网页处理是否真正完成；程序只保证经确认的本地标记操作，不证明论文身份、归属、认领或通讯作者事实。
