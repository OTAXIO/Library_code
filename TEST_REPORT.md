# 验证报告

最新日期：2026-09-25，扩展 0.3.16。PDF 暂停；下列最新验证覆盖历史章节的旧行为描述。

## 操作日志

- 新增本地 `log.txt`，记录桌面操作的时间、名称、名单 ID 和结果。异步网页/Excel/模型操作在回调完成或暂停后写入，普通选单、复制、配对、备注模板操作在执行后写入。
- 以 JSON `operations` 数组的**对象数量**裁剪到最近 100 条，不能按文本行数裁剪。每次写入采用同目录临时文件、刷盘和原子替换；旧文件损坏或替换失败时保留原内容并显示日志保存异常。
- WOS 的检索、导出、上传、导入、回读和推送分别记录，外层仍有整次操作记录。记录失败只提示，不改变已发出的网页请求是否可重试。
- 新增 6 项相关测试，覆盖第 101 条后裁剪、多行操作说明、重启读取、暂停记录、损坏文件保留、替换失败不破坏旧文件、UI 异步成功/失败边界，以及 WOS 分步记录。没有把配对码、密钥、网页正文或异常原文写入日志。

## 确认认领后自动批注结案

- 桌面作者认领页新增默认开启的自动批注选项。确认弹窗明确说明单条认领、自动保存网页“已认领”/已处理、核验后写 Excel 1；不自动选取下一条。只对谭勋策的单匹配、仅作者差异记录适用。
- 只有本次认领的确切人员、署名、序号回读成功才继续；重读 SA、论文和条目快照，除认领与审计字段外不得变化。提交结案前再次读取检查竞态；后台已有其他备注保留，已处理只同步。
- 新增 15 项测试：成功串联、选项冻结、既有备注、日志失败、认领后断连、错误认领证据、其他负责人、字段重复/缺失、SA/论文变化、快照竞态、已处理跳过、不重试。结案与 UI 共 61 项、全量 Python 168 项通过。合成 UI 测试真实读写隔离临时 XLSX，不接触生产名单或网站。
- 自动串联代码的生产验收尚待桌面助手重启后的下一条试验；不能把先前分步完成的两条宣称为此新增自动流程已上线验收。

## 0.3.16 作者选择窗口重建

- 0.3.15 已现场成功读取一条比对详情，人员查询走到关闭选择窗口后被“其他操作窗口”误报拦住；此时尚未认领、审批或回写 Excel。
- 核对生产公开 `views.22dc0ae2.js` 的 AuthorModal 和 `/cdn/element-ui/2.12.0/index.js`：`destroy-on-close` 在关闭时更新内部 key，替换 `.el-dialog`，但保留组件根节点。
- 改为稳定根节点内重新解析可见面板；外来弹窗、标签切换、组件替换、重复面板仍停止。仅在认领结果精确回读成功后恢复自身窗口归属，供下一次只读定位收起；不明结果不自动关闭。
- 新增真实生命周期的合成回归：面板替换后查找/认领、替换同时出现外来弹窗、成功认领后再次只读定位。生产认领是否成功须另行现场验收，不能据离线测试宣称完成。
- 本轮 SA 适配器 86 项、扩展联调 19 项全部通过；仅使用隔离合成页面，未发出生产请求。

### 0.3.16 现场认领与结案入口

- 用户重载后，在谭勋策固定试验范围内完成两条真实单作者认领，两条均由后台精确回读验证学者 ID、作者署名及序号。人员/论文明细仅保留在本地运行日志，不上传 Git。
- 首条认领后再次只读定位也成功。未把认领成功误记为整体完成，尚未回写这两条 Excel 完成标记。
- 桌面新增“网页认领结案”入口，限定负责人、单匹配、唯一作者差异、精确人员编号、已认领状态和人工核对。先检查是否后台已处理；待处理项经再次快照校验后提交一次，网页状态及备注回读通过才同步 Excel。
- 新增 8 项结案逻辑测试、2 项 UI 测试；结案与 UI 合计 46 项通过，全量 Python 153 项通过。真实结案须在重启桌面助手后继续验收；扩展不需再重载。
- 现场结案已验证两条完整链路：认领后重新定位 → 精确回读已认领 → 保存网页“已认领”备注与已处理状态 → 再读后台确认 → 备份并回写 Excel 数字 1。第二条曾在结案前断连，恢复后只继续结案，没有重复认领。谭勋策计数从 280/40 变为 278/42（未完成/已完成）。未操作其他负责人、WOS 导入或 PDF。

## 0.3.15 容器布局兼容

- 0.3.14 生产诊断确认：面板引用是可见抽屉，属于当前组件根节点，有 container 但没有 wrapper。
- 增加依据组件自身面板引用定位唯一外层 container 的分支，排除嵌套抽屉。新增容器布局打开/重读/换条与禁止借用嵌套面板的回归。
- SA 浏览器回归 83 项通过；所有请求由本地合成页面拦截。现场试处理继续使用谭勋策的固定试验范围。

## 0.3.14 现场渲染后结构诊断

- 0.3.13 在生产页面已越过惰性占位检查；详情打开后仍因找不到根内 `.el-drawer__wrapper` 暂停。该详情显示 `rendered=true`、逻辑/UI 已开启、一个全页可见窗口和多个根内窗口；没有执行关闭或业务写入。
- 新增仅含布尔值的面板引用、DOM 归属及容器诊断。未查明真实归属前不把其他抽屉当成详情窗口。

## 0.3.13 现场只读结构修正

- 0.3.12 已连接的生产比对页仍暂停：根 DIV、一个子节点、一个内部窗口，但可见控件/全页可见窗口均为 0，且逻辑与 UI 均未开启；未发生后台写入。
- 仅在逻辑已关闭、组件未渲染、无可见窗口或控件时，容忍保留的旧面板引用或抽屉类根节点；新增正反浏览器测试。SA 合成浏览器 81 项、扩展集成 19 项、工作流 21 项、策略 9 项、WOS 控件 42 项、Python 143 项均通过。生产页面需重载 0.3.13 后再复测。

## 0.3.12 现场只读结构再核验

- 用户重新配对后，确认实际运行 0.3.11；首条只读定位仍报原结构暂停，没有产生网页写入。
- 允许未渲染的一个子元素含隐藏旧抽屉；若有任何可见编辑/确认窗口或交互控件仍停下。新增不含正文的结构计数，后续若仍暂停可精确定位原因。
- SA 合成浏览器回归 79 项通过，新增“已挂载单个隐藏旧抽屉”用例。生产页面需重载 0.3.12 现场复测。

## 0.3.11 现场只读定位修正

- 已登录后台完成固定 100 条试验预检：原本本地已完成 1 条，后台另 99 条仍待处理，Excel 本轮新增同步 0 条；55 条因缺字段或多条匹配被列为搁置待核验。没有执行认领、导入或批准。
- 首条只读定位在 0.3.10 现场出现根节点 DIV、一个子元素、未渲染、无抽屉面板的暂停。新版只在“只读详情 + search”允许最多一个无可见交互控件的占位元素；对话框、其他抽屉、编辑与认领继续阻断。
- SA 合成浏览器回归 78 项通过，其中新增“已挂载、可见、带文字的单个占位子元素”用例；生产网页需要重载 0.3.11 后再次验收。

## 0.3.10 只读详情惰性布局

- 仅在 `rendered=false`、没有面板或 wrapper 且根节点没有可见交互内容时，把含空布局子节点的 DIV 当作惰性占位。可见按钮等内容仍拒绝，写入命令不能据此自动关闭未知窗口。
- SA 浏览器合成页 77 项、扩展与本地桥接 19 项、WOS/导入 21 项、域名与下载策略 9 项、WOS 检索 42 项、Python 143 项通过。测试均未请求生产网站。
- 官方浏览器连接仍报 `nodeRepl.fetch request failed`，本轮未取得登录页面，也没有在真实后台执行认领、导入或完成审批。现场验收尚未完成。

## 0.3.6 空 DIV 占位修复

- 用户提供精确诊断：只读详情根节点为 `DIV`、直属面板 0 个、`rendered=false`。0.3.5 只接受注释节点、未连接节点或已隐藏的 wrapper；在模拟页面按这些条件复现了完全相同的报错。
- 当组件的显示状态明确关闭、尚未渲染且自身 DOM 中没有任何抽屉面板时，允许空根节点，直到 `showItemId` 完成后重新绑定并检查唯一窗口。已打开或存在面板的未知结构仍拒绝。
- 组件根节点和 Element UI wrapper 之间允许一层容器；仅使用属于这个组件的唯一 wrapper 和直属面板。新增两项测试分别覆盖空 DIV 首次打开及嵌套 wrapper 后切换记录。
- 未在用户已登录页面执行实际导入或认领；生产页面是否还存在其他结构差异，仍以重新加载 0.3.6 后的诊断为准。

## 0.3.5 窗口结构兼容性

- 用户反馈 0.3.4 在「只读详情窗口结构不兼容」处暂停。该版本过早要求完整 DOM 和固定内部 ref；本轮允许明确关闭且 `rendered === false` 的占位组件，打开详情后必须重新验证实际窗口。
- 缺少 `$refs.drawer` 时，仅从同一个已识别 wrapper 中查找唯一直属面板；存在冲突 ref、两个面板、未知根节点或处于打开状态的占位组件时仍停止，不能借用另一个窗口。
- 新增 4 项回归，窗口专项共 20 项通过；覆盖延迟首次渲染、私有 ref 缺失后的跨记录关闭、占位状态冲突和多面板歧义。错误增加非敏感结构诊断，避免再次只有泛化报错。
- CN 站实际扩展与本地桥接 19 项隔离往返通过，含单条认领及合成 WOS TXT 导入/推送。语法和 diff 检查通过。
- 只修改程序和合成测试，没有操作生产页面或修改名单；现场具体 DOM 仍未确认，不将兼容性测试表述为生产验收。

## 0.3.4 只读详情关闭修复

- 截图报错来自 SA 的 `adapter.js`，不是 WOS 检索按钮。旧实现每次命令先将详情设为关闭，再等待全页面 `.el-drawer` 数量归零；遇到其他抽屉会泛化报超时，也给同一条记录增加了不必要的关闭/重开过程。
- 只读检查已存档的公开前端代码：`CompareDetailDrawer` 以 `ElDrawer` 为根，嵌套认领、查看、编辑组件；`closed` 回调会清空当前 ID 和详情数组。结合 [Element UI 的 Drawer 实现](https://github.com/ElemeFE/element/blob/v2.15.14/packages/drawer/src/main.vue)，测试夹具补上独立 wrapper、关闭延迟和关闭后清空数据，而非原来的即时 display 切换。
- 先加入连续读取时关闭延迟的回归测试，旧代码复现用户相同错误。此为隔离复现，不证明用户现场就是该延迟；实际现场连接仍报 `nodeRepl.fetch request failed`，没有读取现场 DOM。
- 同一条只读详情直接重新查询，不跳过数据刷新、快照/身份/状态校验；切换记录和完成备注前，按唯一的直属 `ElDrawer` 实例、wrapper、panel 精确定位，调用公开 `closeDrawer()` 一次，同时等待 `closed` 和 DOM 隐藏。未知结构、重新打开、窗口被替换、额外关闭确认仍停止。
- 人工编辑/未提交学者选择和其他可见抽屉不自动关闭；用户自己发起的关闭动画只等待，不重复关闭。只读超时说明本条命令尚未提交写入，已发出业务写入仍保留不重试保护。
- 新增 16 项回归：同条刷新、切条关闭回调、陌生/隐藏抽屉、组件歧义/错配、真实关闭失败、事件与 DOM 双重证据、人工关闭动画、重开、陌生弹窗、未知关闭钩子及未保存编辑。扩展往返新增重复只读查询，默认夹具带关闭动画。
- **103 项测试执行通过**：`npm test` 的 SA/认领适配器 65 项；`npm run test:extension` 在 WOS 国际站和 CN 站各 19 项，均覆盖桌面桥接、重复只读查询、认领以及合成 TXT 上传/导入/推送回读。CN 测试设置 `SA_TEST_WOS_ORIGIN=https://webofscience.clarivate.cn`。语法检查与 `git diff --check` 通过；未重跑未修改的 Python 和 WOS 独立套件。
- 未改真实名单、认领关系、后台记录或 PDF；未调用模型。未完成生产首条验收，0.3.4 不新增权限。

## 0.3.3 文献检索按钮适配

- 用户截图显示字段已切为“标题”且题名已填写，程序在点击检索前暂停。旧逻辑优先取 aria-label，否则拼接含图标的 innerText，要求完整字符串等于 Search/检索/搜索；查找范围仅为最近的 form 或整个 document。
- 隔离页面加入 `<mat-icon>search</mat-icon><span>检索</span>` 后，旧适配器复现完全相同的报错。另覆盖表单外按钮、冗长 aria-label、SVG 图标和重复包装。浏览器观察工具仍返回 `nodeRepl.fetch request failed`，所以这是可复现的程序缺陷，不证明用户现场 DOM 的具体形式。
- 新逻辑去掉装饰图标与隐藏元素后读取按钮文字；无显示文字时才使用精确无障碍名称。按字段/输入所属区域查找，允许显式 HTML form 关联和表单旁动作区；排除导航、其他表单和相似但不同的按钮。多个真实候选、禁用按钮及区域变化仍停止，不取第一个。
- 填写前核验候选，输入事件后重新定位，兼容按钮重绘。错误显示候选数量。只读诊断增加固定按钮标签、图标数量、导航/表单关系和禁用状态；任意按钮文字和 aria-label 遮盖，不输出查询值、账号文案或 Cookie。
- **108 项测试用例执行通过**：新检索按钮套件 `npm run test:wos-search` 在 CN/国际站各 21 项，共 42 项；WOS/导入适配器 21 项；域名/下载策略 9 项；实际 MV3 扩展与本地桥接在两个站点各 18 项，共 36 项。默认合成页也改为表单外、含图标和冗长 aria-label 的按钮，完整链路不再只测试裸文字按钮。
- 没有更改真实 Excel、模型调用、认领逻辑或后台写入校验；没有现场检索、导出、上传、导入或推送。未重跑未修改的 Python/SA 独立测试。0.3.3 较 0.3.2 不新增权限。

## 0.3.2 双域名与绑定修复

- 根据用户截图确认：WOS 地址为 `https://webofscience.clarivate.cn/wos/woscc/basic-search`，已在字段检索页面。旧扩展仅支持 `www.webofscience.com`，绑定校验、主机权限、页面适配器、诊断、检索跳转和下载捕获都有旧域名限制。另一个截图是 `#/item/entryManage` 普通数据管理，而非导入批次页；SA 比对页已连接。
- 现在两个精确 HTTPS 域名均可绑定、诊断、静音、检索和导出；保留当前站点跳转到文献检索，不把 CN 用户自动切回国际站。执行中跨域重定向则暂停。没有开放任意代理站或子域通配权限。
- 下载仍只监听当前导出窗口，验证完整记录路径、referrer 或同源 Blob；另一个 WOS 域名的 Blob、其他论文 referrer、非 TXT、多下载和异常大小仍拒绝。文件上传前的 SHA-256 和单篇身份校验未削弱。
- 将原泛化提示拆分为 SA 标签页复用、WOS 网址不匹配、普通数据管理页和其他非导入页；错误不回显登录参数，失败绑定不清空已有正确绑定。
- 本轮域名/绑定/下载策略 **9 项**通过，WOS/导入适配器 **21 项**通过；实际 MV3 扩展与本地测试桥接在国际站和 CN 站分别 **18 项**通过，均包含从作者入口回到同域文献检索、TXT 导出、哈希核对、模拟上传/导入/推送和回读。
- 原 SA/作者认领适配器 **49 项**回归通过，本轮共运行并通过 **115 项测试用例执行**（含两个域名各自的扩展往返）。未重跑无修改的 Python 桌面测试。
- 测试命令：`npm run test:policy`、`npm run test:workflow`、`npm run test:extension`；将 `SA_TEST_WOS_ORIGIN` 设为 `https://webofscience.clarivate.cn` 再运行扩展测试可验证 CN 分支。
- 全部页面/下载来自隔离浏览器合成数据并拦截网络，未接管用户已登录浏览器，未修改真实名单或数据库。本轮不声称完成生产首条验收。Python 桌面代码未修改。

升级需用户在扩展管理页重新加载到 0.3.2，核对新增 CN 网站权限，刷新工作页并依次重新配对 SA、绑定 WOS、绑定真正的导入批次页。

## 0.3.1 用户报错修复验证

- 用户提供实际 WOS 错误原文 `Oops, something went wrong!`，旧适配器在错误页继续找字段下拉框，从而报告“检索字段选择器未唯一识别”。新版先识别网站错误并暂停，提示按 WOS 的 Search/导航菜单恢复；没有尝试修改远端服务、绕过登录或循环刷新。不能据此确定远端错误的网络/会话/服务端成因。
- 静态复核另发现 All Fields/所有字段遗漏；已补充，并支持通过可见 Advanced Search → Fielded Search 控件进入传统检索。多行、未知控件、非唯一结果仍停止。该分支有离线验证，但不证明就是用户当前页面的直接原因。
- 自动化警告现在使用无声非模态 Tk 提示，保持程序暂停但不锁住主窗口，连续提示复用一个窗口。保留后果性操作的确认，不修改 Windows 音量。用户反映声音可能来自网页，扩展另提供仅对已绑定 WOS 页的静音/恢复按钮；声音实际来源未被测量确认。
- 扩展增加导入管理页入口和只读检查。诊断不包含输入框值、Cookie/登录令牌；在本地扩展窗口显示，不发送到服务器。
- Python 134 项、WOS/导入适配器 20 项通过；其中新增无声非模态窗口、提示更新/关闭后重开、Oops 初始/检索中出现、All Fields 中英文、Smart/Fielded 跳转、多字段拒绝及诊断隐私检查。原 SA 适配器未修改，其 49 项上一轮全部通过。
- 扩展往返 **17 项通过**，新增工作页诊断、限定标签页静音和打开独立导入管理页，原有 WOS 导出到上传/导入/推送链路仍通过。本轮重跑 171 项；加上未修改的 SA 适配器此前 49 项，当前覆盖 220 项。所有测试均为隔离浏览器合成网页，不是生产验收。

真实浏览器观察工具仍连接失败，遵循 computer-use 技能停止现场操作；本轮没有提交真实认领、上传、导入、推送或名单修改。应先使 WOS 恢复正常检索页，再做首条生产验收。

## 2026-09-25 新版验证

- **209 项检查通过**：Python 132 项；原 SA 浏览器适配器 49 项；WOS/导入适配器 14 项；实际扩展 + 本地桥接往返 14 项。所有写入均为合成数据，不访问真实业务接口。
- Python 新增 20 项：问题路由、当前 SA 与旧名单冲突、UTF-8/BOM/UTF-16、带换行的制表符字段、完整记录/单篇约束、交大署名、题名缩写与编号冲突、持久意图、重启恢复、上传/导入/推送不重试、文件篡改、已有批次和用户暂停。测试发现并修复 SQLite 上下文未关闭连接导致的 Windows 文件占用。
- UI：保留 560×700 / 520×600 置顶工作台，新增自动化内的 WOS/作者认领标签，两个子页都验证控件边界。进入页面不发请求，批准 Excel 仍单独勾选和确认。
- 后台模拟按本轮公开 Vue 源码构建：`getData` 不返回 Promise 的异步刷新、导入成功提示只表示提交、未推送批次实际计数为 0、单篇题名/DOI/WOS 回读、同说明批次歧义、手动窗口、表单被改、缺少 PPT 选项、不重复上传/提交均有测试。
- WOS 模拟覆盖单行 DOI/题名检索、唯一结果打开、多结果停下、Full Record、单篇导出、重复导出提交保护和错误路由。
- 实际 MV3 扩展在独立桥接端口完整跑通：SA 配对 → 绑定两个工作页 → WOS 检索 → 捕获对应 TXT 下载 → SHA-256 校验 → 上传一次 → 导入一次 → 批次文献核验 → 优先级合并推送一次 → 推送回读。另验证不匹配文件哈希与改变工作页会拒绝执行。
- 测试环境使用隔离 Edge 配置和专用下载目录。Playwright 默认会把文件保存为无扩展名 GUID，本轮只调整测试浏览器下载目录以保留正常 `.txt` 文件名；未放宽正式程序的文件类型限制。
- 沙箱账号首次运行 Tk/DPAPI 测试因运行库和账号权限失败；在当前 Windows 用户环境重新执行全部 Python 测试成功，没有改变保护策略。

### 不能据此宣称完成的事项

本轮无法取得用户已登录 Chrome/Edge；内置浏览器没有已登录标签，WOS 页面打开超时。因此**未完成生产首条验收**，没有真实 WOS 导出/后台上传/导入/推送/Excel 回写。真实 WOS 控件文本、机构树、批次字段与枚举仍需现场验证；未知状态会停止，而不是猜测继续。

未调用校方模型 API 或发送真实名单。自动分流基于可审计规则；模型页保持用户单条许可和只读辅助。没有 PDF 上传、自动填平台号、自动标记网页完成或修改学者别名。

## 历史验证（2026-09-23，0.2.1）

### 当时的验证

- **170 项检查全部通过**：Python 112 项、浏览器适配器 49 项、真实扩展与本地服务全链路 9 项。此计数不包含未成功的线上模型生成请求。
- Python 包含 16 项单元格回写、31 项界面检查、5 项编号提取、28 项模型接口/密钥/资料验证，其余为核心与规则回归。
- 原生 Tk 窗口在 560×700 和 520×600 两种尺寸核查所有操作按钮、状态和主要控件边界与宽度；置顶开启。配对弹窗按钮也核查过边界。
- 上一轮已用真实名单第 672–674 行的临时副本验证自闭合空备注单元格，连续回写成功；原始文件前后指纹一致。本轮只使用合成名单及合成页面。
- 上一轮名单基线为 3,472 条、26 条数字 1，完成标记是 A 列；后续人工操作会改变计数，以重新读取结果为准。

## 标记失败原因与修复

旧匹配表达式在处理 `<c .../>` 形式的空单元格时，可能把后一个单元格一起匹配，导致负责人等字段改变。回读校验因此拒绝替换原文件，产生用户截图中的错误。

现已使用非贪婪标签匹配，添加该写法的专门回归测试，写入时复制 ZIP 元信息，并验证全部记录只有目标完成标记变化。此问题已经在实际名单格式的临时副本中验证修复，未关闭或削弱安全校验。

## 覆盖的关键分支

- 数字 1 从未完成列表隐藏，可在已完成标签查看；文本 1、0、布尔值保持待办；全部完成时未完成队列可为空。
- 两个备注列正确区分；缺失、重复歧义、公式等异常停止。
- 两个状态标签均只显示当前负责人的任务，旧日志已完成不覆盖 Excel 状态；已完成项绿色显示，不能重复批准。
- 勾选和确认均必需；取消不写入；忙碌时不能切换目标或重复提交。
- 完成后数字回读、绿色状态、移除待办，重启／重读仍隐藏；不会自动搜索或推进下一条。
- 备注模板去重，修改备注或切换记录后清除核验。
- 文件发生变化、Excel 占用、另一助手持锁、保护状态、备份失败和替换失败均停止。
- 替换失败保留原文件，清理本次临时文件；已有备份不删除。
- 连续两次完成刷新文件指纹；原备注可在备份恢复。
- 原始备份逐字节一致；XLSX 内除目标工作表外的所有成员内容不变。样式、长编号、另一备注列、其他工作表与公式保持。
- 保存失败保持待办；保存成功但日志失败显示真实完成结果并提示，不要求重复写入。
- 重读失败清除旧队列，防止继续使用失效名单。
- 启动时不创建浏览器连接；只有用户点击配对才启动。未连接定位会提示；返回 ID 不同则拒绝，不保留可继续编辑的快照。
- 定位使用当前 ID；编辑／认领使用当前查询快照，打开后清除快照；人工界面不接受自动网页完成写入命令。
- 真实扩展能配对、定位和打开确切条目的编辑／认领窗口，开窗不会提交保存。历史自动写入分支仅做离线防回归检查，不从人工界面调用。
- 已完成项可定位网页，但不能由助手打开其编辑／认领窗口；批准按钮只在未完成、勾选核验且非忙碌时可用。

## 新增认领验证

- 中英文括号、前导零、长编号；空白、多号、异常括号、科学计数法及全角数字拒绝，不从本库列或名单 ID 猜工号。
- 人员查询返回唯一总数、新数组、完整文本工号；无结果、多结果、近似号、数值工号、旧查询结果停止。
- 署名精确匹配唯一别名才建议选择；歧义时不预选，允许人核实后指定作者。未选择或取消确认不提交。
- 人员、作者、名单工号或条目变化停止；已有认领、其他有效认领、非本人作品及非本机构行不覆盖。
- 一次确认只写一次，回读确切作者与学者；失败或超时不宣称成功，不重试，不写完成标记。
- 自动认领成功追加本地备注，但 Excel 指纹不变，最终批准仍独立。切换任务清除编号/预览，禁止跨条目复用。
- 在真实扩展往返中复现并修复对象键重排导致的快照误报；测试含字段重排与真正数据变化的区别。
- 在独立随机端口完成实际扩展配对、精确查找、确认认领和回读。修复 Windows 端口可被旧/新助手共用的问题，第二实例明确失败；不结束用户运行的进程。

## 验证边界

- 本轮代码测试使用合成记录和隔离浏览器的合成页面；真实名单未修改，没有提交真实网站认领。另只读核对用户指定的一行名单，并尝试通过浏览器工具打开后台，但未获得可操作的登录页面。
- 窗口检查是原生控件布局与状态验证；尚未由用户在日常浏览器操作中验收各屏幕缩放比例下的体验。
- 扩展测试已重新运行，但离线通过不等于用户已登录网站的现场兼容性验收。扩展版本 0.2.1，未增加权限；旧扩展需重新加载、刷新后台、重新配对。
- 程序验证认领关系回读，不证明论文身份、署名归属或通讯作者事实，最终仍由用户审批完成。

## 选人窗口关闭动画误报

用户提供的错误原文为「网页出现其他操作窗口，请人工处理」。在合成页面加入 Element UI 式延迟隐藏后，旧代码复现出相同错误：`sureAuthor` 已将窗口可见状态改为 false，但 `$nextTick` 不代表关闭动画已经结束，窗口仍可见，触发陌生弹窗保护。

修复只识别并等待本次打开的唯一选人窗口，最多 5 秒。新增 5 项浏览器回归，覆盖正常关闭动画、关闭时出现其他弹窗、查找返回前窗口已完全关闭、窗口持续可见超时、窗口 DOM 无法唯一识别。异常情况下不提交认领，不关闭未知窗口。真实扩展测试也加入关闭动画并通过往返验证。

这是已经复现并修复的一种原因，不等于确认所有生产报错都由动画造成。尚未在用户已登录的生产页面验收，不应因此重试之前写入结果不明的任务。

## 缺失条目与正文试验

已针对用户指定示例找到期刊官方出版页面，通过 nature-downloader 下载一份 CC BY 4.0 正文，并用 PDF 文本提取及首页渲染核对题名、作者、单位、DOI、页数。文件和核验信息仅存于忽略的 `runtime/imports/`，没有上传 Git。

WOS 收录与记录号、本库查重、名单人员对应关系和实际上传控件仍待核实。没有导出 WOS TXT，没有执行数据库导入、推送、全文上传或完成标记。WOS 与 PDF 自动化尚未接入程序。

## 模型辅助页验证

- 三个标签页仍保持默认置顶；560×700 和 520×600 下核查新页输入、许可、模型选择、停止等待、输出区域及按钮边界。没有自动打开浏览器或后台请求。
- 未勾选当前条目发送许可不调用模型。切换记录或修改证据清除旧意见与许可，切换记录清空证据；异步回调拒绝过期资料。
- 模型返回不调用网页或名单写入，不预选人工核验、不批准任务，不自动填入成功备注。合成名单前后文件摘要不变。
- 请求仅访问固定交大 HTTPS 主机与路径；不跟随重定向、不回显 HTTP 正文或异常中的凭据，不自动重试或切换模型。
- 校验原始名单/网页资料范围，过滤工号、平台号等不在允许列表内的网页字段，省略认领括号内数字编号；过长资料停止而不截断。
- 严格校验模型 JSON、结论字段、资料引用和输出长度；截断、工具调用、重复 JSON 键、未知证据引用及损坏响应停止。仅名单或模型指出缺证据时不能显示资料一致。
- 使用合成密钥测试 Windows DPAPI 加解密，确认磁盘密文不包含明文，临时文件已清理。真实密钥单独加密保存在忽略的 runtime，不打印或提交。
- 串行锁、请求间隔、429 冷却、取消等待、响应大小限制均有回归。请求另有 150 秒等待保护；取消可能不停止服务端计费。

### 真实 API 验证边界

已通过用户授权密钥成功读取服务端模型列表，四个界面调用名均可用。随后仅发送一条合成、无原文和人员证据的请求到 `deepseek-reasoner`，服务端返回 **HTTP 429**。已停止，没有重试、改用其他模型或发送真实名单。

因此连接/密钥配置已验证，成功的线上结构化生成和模型判断准确率尚未验证。429 只能说明服务端拒绝当前请求，不能据此断定周额度耗尽。UI 提示用户检查校方限流/额度并手动决定何时再试。
