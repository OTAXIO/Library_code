# WOS 元数据导入与 PDF 补全：待接入流程

状态：流程已依据用户 PPT 第 6–9 页及其中原始截图核对，尚未接入程序或在生产网站执行。当前先修复单条作者认领；不得将本文件当作已实现能力或独立操作授权。

## 首条验收条件

- 用户指定一条未完成、匹配条目数为 0 的名单记录。
- 能操作用户实际登录的后台和机构授权 WOS 页面；新开的未登录浏览器不等同于用户原会话。
- 核对该论文确属交大成果，题名、作者、年份、DOI/WOS ID 等身份信息一致。冲突时询问，不靠题名相似就导入。
- 先在本库前端用正确题名/DOI 查重；原匹配失败不必然代表本库没有记录。已有条目时核实平台唯一号，不新建重复条目。
- 下载范围已经明确：仅 PDF 正文，不获取 SI。下载来源、版本及可使用的许可要留有依据，不绕过机构登录或付费限制。

## 元数据路径

1. 通过用户实际使用的图书馆入口进入 WOS，选择 Web of Science 核心合集。
2. 有准确 DOI 时优先核对 DOI，否则按准确题名检索；检查作者、年份和单位，只选定目标记录。结果不唯一或身份不确定时暂停。
3. 按 PPT 导出「制表符文件」，选择「完整记录」。保留真实 WOS 导出的 TXT，不把其他来源拼接成伪 WOS 记录。
4. 在后台「数据管理 → 数据导入与批次管理」选择「WOS 数据导入（Txt）」。
5. 所属机构选择「上海交通大学」，导入说明按用户确认的格式 `SA补充-当前名单ID`，以实际 sa_lzk 表 ID 替换占位文字。
6. 上传对应 TXT，确认文件上传成功再点击「导入」。检查批次、导入条数和失败条数，不能仅凭上传图标就认为已入库；目标为单条，意外包含多条时停止。
7. 只推送该导入批次，按下表核对设置。不要以另一个批次或全库任务代替。

## 推送选项（PPT 原始截图）

| 项目 | 截图选择 |
|---|---|
| 是否查重 | 是 |
| 查重方式 | 唯一标识 +（期刊 & 发表时间 & 页码 & 卷 & 期 & 题名相似度）+ 题名相似度 |
| 重复条目元数据 | 根据优先级合并 |
| 不存在的条目 | 新增 |
| 是否本校成果 | 是，仅用于已经核实的本校成果 |

推送可能合并已有元数据。正式执行前必须能读回上述选项、确切批次与条数；页面选项变化或权限不明时停下询问，不使用未核实的默认值，也不擅自切换到「整体覆盖」。

推送完成后回读目标论文的平台唯一号和元数据，再回到该条比对任务。不能因看到成功提示就将 Excel 标记为 1，不自动触发全库比对，也不自动合并其他疑似重复项。

## PDF 全文路径

1. 按 DOI/准确题名核实目标；优先寻找合法开放全文，或使用用户合法的机构权限。不能把检索摘要、登录页或 HTML 文件冒充 PDF。
2. 使用 nature-downloader 的正文范围 `--no-si`；按语言、出版商和已授权访问路线执行，不导出 Cookie、密码或会话文件。
3. 检查 PDF 格式、题名、作者、DOI 和版本是否与目标记录一致；来源、文件摘要和访问日期仅记录在本地运行目录，不上传 Git。
4. 在确切目标条目中上传正文。全文版本、使用许可、访问范围不明，或没有可上传的合法版本时询问用户，不自动选择「公开」。
5. 保存后回读该条目的附件信息。网络超时或保存结果不明时停止，不重复上传；最终仍由人工批准任务完成。

## 认领相关补充分支

PPT 第 9 页还包含学者别名维护：研究单元/用户管理 → 学者管理 → 工号检索 → 别名 → 新增并保存。不能因为姓名匹配失败就自动给学者加别名。需要先核实该署名确实属于该人员，再确认是否维护别名；不修改首选中文名/英文名或删除已有别名。

## 当前阻塞

用户已指定首条缺失记录，并给出后台和 WOS 地址。已找到一份官方开放获取正文并完成本地核验，记录明细只保存在忽略的运行目录中。名单只含工号、没有作者姓名，人员与论文的对应关系仍待确认。

当前浏览器工具未识别用户已登录的 Chrome/Edge，内置浏览器打开后台后在 `#/404`，页面读取超时。需要可操作的登录页面后再进行现场接入；单纯获得 URL 或配对本地助手，不代表 Codex 的浏览器工具已经连接用户会话。用户提供的 WOS 地址为作者搜索入口，应在文献检索中核实目标。若暂时无法接通，可由用户导出目标单条的完整记录 TXT 供只读检查，但不能用它跳过本库查重、目标核对和推送确认。
