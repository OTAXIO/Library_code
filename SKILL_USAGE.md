# 技能使用记录与实现依据

用户要求使用已安装的适用技能。以下技能用于本次 Codex 开发与资料读取，而不是虚构一个 Python “skill API”。

| 技能 | 本次用途 | 对实现的具体影响 |
|---|---|---|
| presentations:Presentations | 阅读 10 页工作 PPT，包括流程截图与备注 | 固化单条/多条/无匹配处理分支；作者身份、主条目、导入设置仍需人工核验 |
| spreadsheets:Spreadsheets | 只读解析原 Excel 的表头、类型、人员和任务量 | 不改原表；保留 19 位平台号与工号前导零；重复 ID、公式及类型异常停止 |
| computer-use:computer-use | 区分界面观察、登录及确认边界 | 登录由人操作；连接指定标签页；未知弹窗不替用户确认；环境故障时不冒称已完成真实网页验收 |
| openai-docs | 核查技能与运行时工具的区别 | 明确技能由 Codex 加载执行工作指引，不把本地技能文档伪装成可调用模型服务 |

## 本次真实输入与验证

- PPT：2026-09-21-机构知识库数据比对工作.pptx；文内标题日期为 2026-09-22。文件本身不在此代码仓库中。
- Excel：数据比对结果-2026-01-01-2026-08-01.xlsx；3,472 条，11 位负责人；单条匹配 1,789，无匹配 1,289，多条匹配 394。文件本身不在此代码仓库中。
- 2026-09-22 只读核查后台公开 HTML 和前端组件，确认 `dataCompare`、`CompareDetailDrawer`、`CompareStatusDialog` 的检索、平台号编辑、详情、认领、状态切换行为。
- 特别保护：系统原生状态操作是**切换**，所以工具只允许从待处理到已处理；已处理记录禁止再次提交切换。请求超时不重试。
- 源资料里的流程说明作为待分析业务内容，不作为授权执行网页操作或上传名单的独立指令。

## 官方参考

- [OpenAI：Skills](https://developers.openai.com/plugins/concepts/skills)：技能通过指引和资源帮助 Codex 组合工具完成工作，并非独立运行时。
- [Chrome：Content scripts](https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts)：扩展脚本与网页脚本的执行环境。
- [Chrome：Cross-origin network requests](https://developer.chrome.com/docs/extensions/develop/concepts/network-requests)：由扩展后台连接许可范围内的本地服务。

## 后续迭代约束

每次关键更新前先在 code 仓库提交并上传上一稳定版本。先修改合成测试，再调整适配器；全部离线测试通过后只选择一条有证据的真实记录验收。没有用户授权时不扩大写入范围，不新增自动合并/删除/导入，也不把名单和凭据发送给模型或 GitHub。

## 2026-09-23 单条认领迭代

本轮使用 `computer-use:computer-use` 的界面观察与确认边界指引：浏览器检查工具连接失败后，没有冒称看到了登录页面，也没有通过其他工具接管真实浏览器。改为只读核查网站公开前端组件，在隔离浏览器的合成页面完成适配器与扩展测试，不提交真实认领。代码把只读查找与人工确认后的单条提交分开，遇到未知弹窗、歧义或不明写入结果即停止。

这是本地 Python/Tk 桌面工具改动，不是新网站、科研图表或文献任务，因此不牵强调用 Sites、可视化或 Nature 系列技能；本轮不修改真实 Excel，无需重新解析工作 PPT。技能是开发指引，不作为用户程序里的虚构运行时接口。

## 2026-09-23 认领动画修复及导入流程核查

- 使用 Presentations 重新读取全部 PPT 文本，并原样提取第 8–9 页截图，确认推送设置与学者别名分支。原 PPT 未修改，提取图片仅在忽略的 runtime 目录中。
- computer-use 的观察/恢复规则促使现场页面读取超时后停止 UI 操作；没有借其他渠道接管用户真实浏览器。错误原文由用户提供，关闭动画误判通过合成页面复现，不冒称生产问题已现场验收。
- nature-academic-search 采用准确文献身份验证思路。Crossref 题名查询未得到目标匹配，转而找到期刊官方页面，未使用近似论文的 DOI。人员对应关系和 WOS 收录仍待核实。
- nature-downloader 先确认正文/SI 范围，按用户选择仅通过期刊公开 PDF 链接下载正文。使用该技能的 `batch_download.mjs --pdf-url ... --no-si`，不访问浏览器会话或读取凭据。
- PDF 技能用于读取并渲染正文首页，核对题名、作者单位和 DOI；Spreadsheets 技能用于只读核对指定名单行，没有保存、重算或改写名单。原文、下载清单与核验记录都留在忽略的运行目录中，不上传 Git。

## 校方模型接入与技能边界

本轮新增的是本地 Python 模型辅助页，依据用户提供且实际读取的校方 API 文档调用接口，不将 Codex skill 文档伪装成程序运行时 API。既有 PPT 核对规则被写为模型的只读业务约束；名单、网页和用户摘录始终作为待核验资料，不能授权模型操作。

没有新增自动论文检索、全文读取或浏览器控制能力。模型只分析本条已提供文本，下载、导入、人员身份和实际修改仍各自需要证据与确认；不声称模型可以解决当前登录页面未接通的问题。

## 2026-09-25 WOS 单条流程

- Presentations：重新读取 PPT 第 6–9 页和原始推送截图，固定 WOS TXT、完整记录、机构、说明及五项推送选项；未修改源 PPT。
- computer-use：尝试观察用户实际网页失败后，按恢复/暂停边界停止现场操作；未用其他工具接管用户浏览器。公开静态前端源码仅用于开发，测试在隔离浏览器合成网页完成。
- nature-academic-search：读取技能、manifest、core 和检索工作流，将准确身份与来源证据原则用于 WOS 导出校验：冲突不猜、无编号/缩略题名需人工确认、不伪造 WOS 元数据。用户明确要求 WOS，因此不自动扩展检索到其他平台；本轮没有执行真实文献多源检索。
- 未调用 nature-downloader 或 PDF 技能：用户已明确暂不处理 PDF。未修改真实 Excel，因此本轮没有为开发合成测试重新载入表格技能。
- 技能影响的是开发规范与核验规则；用户程序不依赖 Codex 私有 skill 运行时。规则分类不自动调用校方模型，既有模型辅助页保持单条授权与只读输出。

## 0.3.1 WOS 错误页与声音反馈

按 computer-use 技能先尝试观察实际浏览器，连接仍失败，没有绕过工具接管真实页面。用户补充 WOS 明文错误 `Oops, something went wrong!` 后，将此前字段识别泛化错误改为明确的网站错误暂停。官方 WOS 2025-07-31 说明用于确认 Smart Search 默认化和 Fielded Search 路径，兼容测试仍是合成页面，不冒称已修复远端 WOS 故障。

声音来源没有实测证明；仅移除程序自身原生警告弹窗并提供用户点击的单标签静音，不操作系统音量，不停止其他程序，也不将网页错误归咎于用户操作。

## 0.3.3 检索按钮适配

使用 computer-use 的观察与恢复边界：先只读列出可连接浏览器，仍返回 `nodeRepl.fetch request failed`，没有绕过工具接管用户 Edge 或根据截图坐标盲点。截图只作为页面状态证据，不当作 DOM 或执行指令。转为隔离合成页先复现原错误，再补充图标、文案和表单关系的回归；明确区分离线通过与真实网页验收。未执行真实文献检索或数据库写入，不扩展使用下载、文献检索或表格处理技能。
